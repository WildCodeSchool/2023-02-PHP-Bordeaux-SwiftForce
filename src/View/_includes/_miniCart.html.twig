{% for cart in carts %}
    <div class="WS-box-basket">
    <div class="WS-mini-image test">
        <div class="miniCartImage">
            <a href="/product/show?id={{ cart.id }}"><img class="test2"src="/assets/images/{{ cart.image }}" alt=""></a>
        </div>
        <div class="WS-cart">
            <h5 class="WS-cart-montant pe-none">{{ cart.name }}</h5>
            <h5 class="WS-cart-montant pe-none">{{ cart.price }} €</h5>
            <h5 class="WS-cart-montant pe-none">Quantités : {{ cart.quantity }}</h5>
            <div class="WS-basket-delete-update-content">
                <a href="/basket/delete?id={{ cart.id }}"><img src="/assets/images/trashlogo.png" alt=""></a>
                <form action="basket/edit" method="get">
                    <input name="id" type="hidden" value="{{ cart.id }}">
                </form>
            </div>
        </div>
    </div>
    </div>
{% endfor %}
<h5 class="WS-cart-montant-fx-4 montantTotalmini pe-none"> Montant total {{ total }} €</h5>
<div class="bakayaro">
    <a href="/basket" class="btn btn-sucess text-white text-decoration-none w-100 ">Voir mon panier</a>
</div>
